node_modules/
dist/
# Both of these were excluded in .git/info/exclude, which is local to one disk and
# never cloned. The directories are nested git repositories with their own
# remotes, so no content was at risk - but the decision to keep them out of this
# repo lived in exactly one place, and a fresh clone would not have inherited it.
refer-zo-bootstrap/
.claude/worktrees/
# VS Code Local History. It snapshots whatever is open, including files that are
# ignored everywhere else, and it was tracked here rather than ignored - so on
# 2026-06-17 the commit named "chore: repo safeguard snapshot" pushed two
# alliance-hub/.dev.vars snapshots to a PUBLIC repo. What those two held was mild
# (a local placeholder secret and one phone number). What they would hold today
# is not: that same file now carries SUPABASE_SERVICE_ROLE_KEY, OPEN_AI_CHAT_KEY
# and ALLIANCE_SMS_RELAY_TOKEN.
#
# The .env* rules below never covered it, because a .dev.vars is not a .env. The
# hole was never the pattern list - it was that an editor was allowed to choose
# what enters the repo.
.history/
# Ephemeral git worktrees OF the alliance-hub submodule, one per in-flight lane,
# parked in this tree for convenience. They are not this repo's content and they
# are not submodules - a worktree's `.git` is a file pointing back into the parent
# repo, so it cannot be cloned independently.
#
# One of them, `agents-stormy-guanaco`, had been committed as a submodule gitlink
# with a `.gitmodules` entry pointing at the alliance-hub remote. A fresh clone
# running `git submodule update` would have checked alliance-hub out a SECOND
# time, at whatever branch that agent lane happened to be on, into a directory
# meant to be disposable. Same reasoning as `.claude/worktrees/` above.
alliance-hub.worktrees/
alliance-hub/.deploy/
alliance-hub/.wrangler/
*.vsix
.DS_Store
npm-debug.log*
.env
.env.*
.env*
.refer-factory/*.local.json
.refer-factory/process-state.json
# The pulse belt is universal factory state, rewritten every tick by the one
# machine that owns it. Registries under .refer-factory are tracked; a heartbeat
# is not a registry, and "dirty has to mean something".
.refer-factory/pulse-belt.jsonl
.refer-factory/pulse-belt.json
.refer-factory/updates/
zo-computer/usage/*.jsonl
# Same class as the line above, in the other directory it was written to. Nothing
# in this repo has written it since 2026-05-01.
usage/*.jsonl
# Chromium crash noise from an Electron host, 316 bytes of it, tracked since
# April. npm-debug.log* above did not match the name.
debug.log
# THE FACTORY IS TICKED AND HAS A BOARD OF ITS OWN, since 2026-09-14. The belt
# (findings.jsonl) and the ledger are tracked, as Telechurch tracks them: they
# are the record. Everything else under agent-context is a machine's own output,
# rewritten every run - the same list Telechurch ignores, for the same reason:
# "dirty has to mean something".
.claude/agent-context/schedule-state.json
.claude/agent-context/clock-state.json
.claude/agent-context/pulse.json
.claude/agent-context/autonomy.json
.claude/agent-context/manager.json
.claude/agent-context/watcher-queue.json
.claude/agent-context/handles.json
.claude/agent-context/board-counts.json
.claude/agent-context/board-read.json
.claude/agent-context/board-port.txt
.claude/agent-context/board-snaps/
.claude/agent-context/board-see.json
.claude/agent-context/board-see-detail.json
.claude/agent-context/factory-tracker.html
.claude/agent-context/exit-worker.json
.claude/agent-context/intake-worker.json
.claude/agent-context/intake-brief.txt
.claude/agent-context/provider-watch.json
.claude/agent-context/mind-watch.json
.claude/agent-context/mind-baseline.json
.claude/agent-context/composition-watch.json
.claude/agent-context/composition-baseline.json
.claude/agent-context/host-restart.json
.claude/agent-context/night-report.txt
.claude/agent-context/night-watch.json
.claude/agent-context/night-watch.mark.json
