# PLAN-REFER-FACTORY-001 Bootstrap Handoff

Build only the first slice:

`REFER Factory extension shell + Live/Average Miles/MPG/gears dashboard + live process panel + repo bootstrap dry-run/apply + Send Contract draft schema + adapter contract + agent governance bootstrap`

Do not build hosted sync, marketplace, billing, cloud orchestration, or full
autonomous execution.

The extension must contribute:

- activity bar/container: `REFER`
- dashboard webview
- process panel with governed events
- bootstrap library panel with REFER bootstrap source reference inspection
- command: `REFER: Initialize Repo` with explicit apply confirmation
- command: `REFER: Emit Send Contract Draft`
- command: `REFER: Emit Script Blueprint`
- command: `REFER: Emit Script DNA Seed`
- command: `REFER: Refresh Codebases`
- command: `REFER: Check for Updates`
- command: `REFER: Apply Update`
- bootstrap output: `AGENTS.md` with an append/update-only REFER governance block
- bootstrap output: `.refer-factory/agent-profile.json`
- bootstrap output: `.refer-factory/codebases.json` with repo-level tracking and
  refreshable internal subspace discovery
- bootstrap output: `.refer-factory/plan/refer.plan.json`
- update sync: channel-based manifest check, notification, explicit apply,
  backup, and collapsed process-state evidence

Telechurch is only the pilot consumer. Do not import Telechurch app code.
